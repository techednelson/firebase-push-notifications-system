export enum NotificationType {
  TOPIC = 'TOPIC', MULTICAST = 'MULTICAST', SINGLE = 'SINGLE',
}

export enum NotificationStatus {
  IN_PROGRESS = 'IN_PROGRESS', COMPLETED = 'COMPLETED', FAILED = 'FAILED'
}

export enum StepperStatus {
  INITIAL = 'INITIAL', VALIDATING = 'VALIDATING', VALID = 'VALID', INVALID = 'INVALID',
}

